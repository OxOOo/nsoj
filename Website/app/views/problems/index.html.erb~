<table class="table table-striped table-bordered table-hover">
	<thead>
		<tr>
			<th class="text-center" style="width:5em;">编号</th>
			<th>题目名称</th>
		</tr>
	</thead>
	<tbody>
		<% if @problems.any? %>
			<% @problems.each do |problem| %>
				<tr>
					<td class="text-center"><%= link_to "#{problem.id}", problems_single_path(problem) %></td>
					<td><%= link_to problem.title, problems_single_path(problem) %></td>
				</tr>
			<% end %>
		<% else %>
			<tr>
				<td class="text-center" colspan="2">无</td>
			</tr>
		<% end %>
	</tbody>
</table>
