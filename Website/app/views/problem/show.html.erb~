<h1></h1>
<div class="container-fluid">
	<div class="page-header text-center">
		<h1><%= @problem.name %></h1>
		<p><small>时间限制： </small><%= friendly_time(@problem.time_limit) %>  <small>空间限制： </small><%= friendly_memory(@problem.memory_limit) %></p>
		<p><small>源代码（答案文件）大小限制： </small><%= friendly_memory(@problem.answer_limit) %></p>
	</div>
	<div class="panel panel-default">
  	<div class="panel-body">
  		<%= @problem.description %>
  	</div>
	</div>
	<% if @problem.new_record? == false %>
		<div class="text-right">
			<button type="button" class="btn btn-default" data-toggle="modal" data-target="#submit_modal">提交</button>
		</div>

		<!-- Modal -->
		<div class="modal fade" id="submit_modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
			<div class="modal-dialog modal-lg">
				<div class="modal-content">
				  <div class="modal-header">
				    <h1><%= @problem.name %></h1>
				  </div>
				  <div class="modal-body">
				    <%= form_for :submission, url: submission_submit_path(@course_problem_ship.course,@course_problem_ship.problem), html: {class: 'form-horizontal', role: 'form'} do |f| %>
							<fieldset>
								<legend>提交代码</legend>
								<div class="form-group">
									<label class="col-sm-2 control-label">运行环境：</label>
									<div class="col-sm-3">
										<%= f.select :environment_type, @problem.vaild_environments.collect{ |p| [ p.name, p.id ] }, {}, class: "form-control" %>
									</div>
								</div>
								<div class="form-group">
									<label class="col-sm-2 control-label">语言：</label>
									<div class="col-sm-3">
										<%= f.select :language, Language::List.collect{ |p| [ p.name, p.id ] }, {}, class: "form-control" %>
									</div>
								</div>
								<div class="form-group">
									<label class="col-sm-2 control-label">代码：</label>
									<div class="col-sm-10">
										<%= f.text_area :source, class: "form-control", rows: "15" %>
									</div>
								</div>
								<div class="form-group">
									<div class="col-sm-offset-2 col-sm-10">
										<%= f.submit '提交', class: 'btn btn-default' %>
										<button type="button" class="btn btn-primary pull-right" data-dismiss="modal">关闭</button>
									</div>
								</div>
							</fieldset>
						<% end %>
				  </div>
				</div>
			</div>
		</div>
	<% end %>
</div>

