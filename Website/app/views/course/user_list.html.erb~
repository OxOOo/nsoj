<h1></h1>
<table class="table table-striped">
	<thead>
		<tr>
			<th>用户名</th>
			<th>通过的题数</th>
			<th>通过的次数</th>
			<th>提交的次数</th>
			<th>加入课程的时间</th>
		</tr>
	</thead>
	<tbody>
		<% @user_course_ships.each do |user_course_ship| %>
			<tr>
				<td><%= link_to user_course_ship.user.username, user_path(user_course_ship.user) %></td>
				<td><%= user_course_ship.solved.size %></td>
				<td><%= user_course_ship.accepted.size %></td>
				<td><%= user_course_ship.submissions.size %></td>
				<td><%= format_time(user_course_ship.created_at) %></td>
			<tr>
		<% end %>
	</tbody>
</table>
<div class="text-center">
	<%= will_paginate @collection, renderer: BootstrapPagination::Rails %>
</div>

