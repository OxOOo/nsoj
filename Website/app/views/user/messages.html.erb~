<h1></h1>
<table class="table table-striped">
	<thead>
		<tr>
			<th></th>
			<th>发信者</th>
			<th>收信者</th>
			<th>标题</th>
			<th>发信时间</th>
			<th>读信时间</th>
		</tr>
	</thead>
	<tbody>
		<% @messages.each do |message| %>
			<tr>
				<td><%= raw (message.read==true)?('<span class="label label-success">已读</span>'):('<span class="label label-default">未读</span>')%></td>
				<td><%= message.from_user.username %></td>
				<td><%= message.receive_user.username %></td>
				<td><%= message.title %></td>
				<td><%= format_time(message.created_at) %></td>
				<td><%= (message.read==true)?format_time(message.updated_at):("未读") %></td>
			</tr>
		<% end %>
	</toby>
</table>
<div class="text-center">
	<%= will_paginate @messages, renderer: BootstrapPagination::Rails %>
</div>

<%= render 'send_message' %>
