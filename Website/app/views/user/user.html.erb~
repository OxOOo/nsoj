<h1></h1>
<div class="container-fluid">
	<div class="row">
		<div class="col-md-8">
			<h4 class="text-center">基本资料</h4>
			<dl class="dl-horizontal">
				<dt></dt>
				<dd>第 <%= @current_user.id %> 位用户</dd>
				<dt class="small">用户名：</dt>
				<dd><%= @user.username %></dd>
				<dt class="small">真实姓名：</dt>
				<dd><%= @user.realname %></dd>
				<dt class="small">邮箱：</dt>
				<dd><%= @user.email %></dd>
				<dt class="small">学校：</dt>
				<dd><%= @user.school %></dd>
				<dt class="small">权限等级：</dt>
				<dd><%= @user.level_info %></dd>
				<dt class="small">通过题数：</dt>
				<dd><%= @user.solved.size %></dd>
				<dt class="small">通过次数：</dt>
				<dd><%= @user.accepted.size %></dd>
				<dt class="small">提交次数：</dt>
				<dd><%= @user.submissions.size %></dd>
			</dl>
		</div>
	</div>
</div>
